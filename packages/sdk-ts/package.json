{
  "name": "@skillspace/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "SkillSpace TypeScript SDK",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@skillspace/runtime": "workspace:*",
    "@skillspace/schema": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.6"
  }
}
