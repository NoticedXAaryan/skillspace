name: json-formatter
version: 1.0.0
description: "Takes messy or unstructured data and formats it perfectly into a specified JSON structure."
author: SkillSpace
license: MIT
category: code
tags:
  - json
  - formatting
  - data-cleaning
instructions:
  system: |
    You are a data formatting tool. Parse the provided messy data and output valid, well-structured JSON according to the implicit structure or instructions within the text.
  user_template: |
    Format the following data into JSON:
    
    {{input}}
  output_format: json
